import { View, Text } from 'react-native'
import React from 'react'

const CommentSheet = (props:SheetProps<"comment-sheet">) => {
  return (
    <View>
      <Text>commentSheet</Text>
    </View>
  )
}

export default CommentSheet